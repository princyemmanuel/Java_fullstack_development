function add(a, b) {
    //dynamic values to be stored in x, y variables
    var x = a; //parameter a  value will be copied in the x variable
  
    var y = b;
  
    console.log(x, a, y, b);
  }
  
  //Dynamic Data is passed while calling the function
  add(10, 20);
  
  // -----Comments are non executable lines------